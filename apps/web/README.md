# apps/web

This is the canonical production web app for Kinetix (`@kinetix/web`) and the current source for `kinetix.bookiji.com`.

- Status: **canonical / active production surface**.
- New production web work here: **Yes**.
- Relationship: consumes shared logic from `packages/core` and integrates with root `/api` serverless routes plus optional `apps/rag` services.
