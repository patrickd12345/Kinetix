# packages/core

Shared Kinetix core package (`@kinetix/core`) for scoring/math and reusable domain logic.

- Status: **shared canonical package**.
- New production work here: **Yes, for shared logic only** (not UI/deployment-specific behavior).
- Relationship: consumed by `apps/web` and intended as the convergence point for cross-surface core logic.
