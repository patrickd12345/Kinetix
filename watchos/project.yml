name: KinetixWatch
options:
  bundleIdPrefix: com.kinetix
  deploymentTarget:
    iOS: "17.0"
    watchOS: "11.0"
  developmentLanguage: en
  xcodeVersion: "15.0"

targets:
  KinetixCompanion:
    type: application
    platform: iOS
    sources:
      - path: KinetixCompanion
        excludes:
          - "**/*.app"
          - "**/*.ipa"
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.patrickduchesneau.KinetixCompanion
      INFOPLIST_FILE: KinetixCompanion/Info.plist
      SWIFT_VERSION: "5.0"
      TARGETED_DEVICE_FAMILY: "1"
      IPHONEOS_DEPLOYMENT_TARGET: "17.0"
      DEVELOPMENT_TEAM: ""
      SUPPORTED_PLATFORMS: iphonesimulator iphoneos
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      CODE_SIGN_STYLE: Automatic
  KinetixWatch:
    type: application
    platform: watchOS
    sources:
      - path: KinetixWatch
        excludes:
          - "**/*.app"
          - "**/*.ipa"
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.patrickduchesneau.KinetixWatchOS
      INFOPLIST_FILE: KinetixWatch/Info.plist
      CODE_SIGN_ENTITLEMENTS: KinetixWatch/KinetixWatch.entitlements
      SWIFT_VERSION: "5.0"
      TARGETED_DEVICE_FAMILY: "4" # watchOS
      WATCHOS_DEPLOYMENT_TARGET: "11.0"
      DEVELOPMENT_TEAM: ""
      SUPPORTED_PLATFORMS: watchos
      CODE_SIGN_CONTEXT_CLASS: XCWatchOSCodeSignContext
    capabilities:
      - Background Modes

schemes:
  KinetixCompanion:
    build:
      targets:
        KinetixCompanion: all
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
  KinetixWatch:
    build:
      targets:
        KinetixWatch: all
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
