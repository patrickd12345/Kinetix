# web

This folder is a contract placeholder for historical references to a top-level `web/` app path.

- Status: **legacy path marker / migration-source alias only** (no active app implementation here).
- New production web work here: **No**.
- Relationship: live and canonical web work is in `apps/web`; older legacy implementation references are kept in `archive/web-legacy`.
